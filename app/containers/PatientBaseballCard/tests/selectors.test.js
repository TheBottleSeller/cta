// import { selectPatientBaseballCardDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectPatientBaseballCardDomain();

describe('selectPatientBaseballCardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
